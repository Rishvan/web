/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    // images: {
    //     domains: ["localhost"], // Add your image domains here
    //   },
};

export default nextConfig;
